# Explore VQE algorithm

## Goal: Familiarize with VQE algorithm. Better understand its moving parts.



### With Nvidia GPU
```
nvq++ main.cpp -o main.x --target nvidia && ./main.x
```

### Without Nvidia GPU
```
nvq++ main.cpp -o main.x && ./main.x
```